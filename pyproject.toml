[project]
name = "activity-harness"
version = "0.1.0"
description = "macOS red-team activity harness for testing the monitoring agent presence detection."
requires-python = ">=3.13"
dependencies = [
    "pyautogui>=0.9.54",
]

[dependency-groups]
dev = ["pytest>=8.0"]
